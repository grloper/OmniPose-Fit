package com.grloepr.pushtrack.ui.components

import androidx.compose.runtime.Composable

/**
 * Removed legacy complex UI. Keep placeholder to satisfy imports if any remain.
 * TODO: Rebuild richer UI if needed.
 */
@Composable
fun PlaceholderRemovedUI() { /* Intentionally empty */ }